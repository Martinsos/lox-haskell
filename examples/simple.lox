// Example program!

var name = "Hloxy";
var age = 42;

print name;
print age;

if (age < 65) {
  print (name + " is not yet retired");
} else {
  print (name + " is probably retired");
}

print ("Shutting down in:");
var count = 3;
while (count > 0) {
  print count;
  count = count - 1;
}
